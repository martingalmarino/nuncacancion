---
import ReadingModeToggle from "./ReadingModeToggle.astro";
import { site } from "../config/site";
---

<header class="site-header">
  <a class="wordmark wordmark-link" href="#inicio">
    <span class="wordmark-live">{site.wordmark}</span>
    <span class="wordmark-ghost wordmark-ghost-a" aria-hidden="true">{site.wordmark}</span>
    <span class="wordmark-ghost wordmark-ghost-b" aria-hidden="true">{site.wordmark}</span>
  </a>
  <div class="header-tools">
    <ReadingModeToggle />
    <a class="header-index" href="#escritos">Escritos</a>
  </div>
</header>
